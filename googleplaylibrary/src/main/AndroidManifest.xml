<manifest package="com.example.chu.googleplaylibrary"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        >
        <activity android:name=".ui.FullScreenPlayerActivity">
        </activity>

        <activity android:name=".ui.ActionBarCastActivity"/>
        <activity android:name=".ui.MusicPlayerActivity"/>
        <activity android:name=".ui.NowPlayingActivity"/>
    </application>

</manifest>
